{
  "name": "phone-boxes-app",
  "version": "1.0.0",
  "description": "Mobile swipe boxes demo using IndexedDB and TypeScript",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
